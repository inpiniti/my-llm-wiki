---
# ── 필수 ──────────────────────────────────────────────
title: 항목 제목 — 무엇을 다루는가
tldr: 한두 문장 요약. 카드와 상세 헤더에 그대로 노출된다.
status: open          # solved | wip | open  (해결됨 | 진행 중 | 열린 문제)
difficulty: 2         # 1 | 2 | 3  (초급 | 중급 | 고급)
tags: [예시태그, another]

# ── 선택 ──────────────────────────────────────────────
project: 관련 프로젝트 이름
date: 2026-05-21       # 발견/작성 시점
impact: 이 문제가 미친 영향 한 줄
decision: 최종적으로 내린 결정 한 줄
related: [other-slug]  # 연결된 항목 slug 배열
# order: 1             # 지정하면 정렬·번호가 이 값 우선 (없으면 date 최신순)
---

본문은 표준 Markdown + 아래 커스텀 컴포넌트를 쓸 수 있다.
섹션은 `##` 헤딩으로 나눈다. 권장 흐름: 관찰 → 원인 → 해결 → 남은 의문.

## 관찰 / 문제

무슨 일이 있었는지. 재현 조건, 증상, 로그.

## 왜 그런가 / 원인

근본 원인. 추측이라면 추측이라고 적는다.

## 해결

실제로 한 것. 코드는 펜스로:

```ts
// 예시
```

<Callout label="결론">
핵심 교훈을 한 단락으로. 다음에 같은 상황에서 바로 꺼내 쓸 수 있게.
</Callout>

## 남은 의문

아직 안 풀린 것. 검증 못 한 가설. 별도 항목으로 분리할 후보.

<Sidenote>
여담·맥락·참고 링크처럼 본문 흐름에서 살짝 비켜난 메모는 여기에.
</Sidenote>
